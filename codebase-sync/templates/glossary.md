# {{PROJECT_NAME}} — Glossary (Canonical Names)

> ใช้ชื่อตามนี้เสมอ — ห้ามคิดชื่อใหม่ ห้ามสร้าง variant ซ้ำ
> ถ้าต้องการของที่ไม่มีในนี้ → เพิ่มเข้า list ก่อน อย่าสร้างมั่ว

## Components
{{COMPONENT_GLOSSARY}}

## Design Token Prefixes
{{TOKEN_GLOSSARY}}

## Naming Conventions
- Component: `PascalCase` (Button, CourseCard)
- Feature action: `{module}{Method}{Action}` ({{ACTION_EXAMPLE}})
- CSS variable: `--{group}-{name}` (--bg-card, --text-muted)
